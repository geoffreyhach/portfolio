import React from "react";
import PageTitle from "../components/PageTitle";

const Projects = () => {
    return (
        <section className="full-page projects bg-primary">
            <PageTitle text="projects" classes="color-white" />
        </section>
    );
};

export default Projects;
